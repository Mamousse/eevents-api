# MongoDB Configuration (REQUIS)
MONGO_URL="mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&appName=AppName"

# Server Configuration (REQUIS)
PORT=3000
HOST=0.0.0.0

# Node Environment (REQUIS)
NODE_ENV=production

# JWT Secret (OPTIONNEL - mais recommandé en production)
# Si non défini, une valeur par défaut sera utilisée (pas sécurisé pour la production)
# JWT_SECRET="votre-secret-key-super-securisee-changez-moi-en-production"
